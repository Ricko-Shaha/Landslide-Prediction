# Versions that load the shipped model without scikit-learn compatibility warnings.
# Keep the model, metrics and precomputed map together when updating these.
Flask==3.1.3
joblib==1.5.3
numpy==2.4.4
pandas==3.0.5
scikit-learn==1.9.1
scipy==1.17.1
waitress==3.0.2
